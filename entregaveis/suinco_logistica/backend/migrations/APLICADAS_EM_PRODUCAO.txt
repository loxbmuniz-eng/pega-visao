048

# ---------------------------------------------------------------------
# ATÉ QUAL MIGRAÇÃO O SERVIDOR DE PRODUÇÃO ESTÁ.
#
# A primeira linha é o número, e só ela é lida. Tudo acima dela é
# considerado PENDENTE pelo portão de publicação (publicar.sh), que passa a
# imprimir a consequência de cada uma até que subam.
#
# POR QUE ESTE ARQUIVO EXISTE. O portão sabia detectar migração nova NESTA
# publicação, e não migração NÃO APLICADA. Eram coisas diferentes: a 035 e
# a 036 subiram para o repositório em 25/08, continuaram sem rodar no
# servidor, e o portão as consideraria resolvidas por já não serem novas —
# repetindo em forma de script exatamente o esquecimento que ele existe
# para impedir.
#
# QUEM ATUALIZA, E QUANDO. Depois de rodar o atualizar.sh no servidor e ver
# a confirmação, o número aqui sobe para a última migração aplicada. Nunca
# antes: este arquivo é registro do que ACONTECEU, não do que se pretende
# fazer. Subir o número "porque já vai rodar" transforma o controle em
# enfeite.
#
# COMO CONFERIR o que o servidor tem de verdade:
#     ssh root@2.25.95.253
#     su postgres
#     psql -d embarque_suinco -c "SELECT arquivo FROM _migrations ORDER BY arquivo DESC LIMIT 5"
#     exit
#
# (A tabela chama-se _migrations e a coluna, arquivo. Estava escrito
#  "migracoes"/"nome" aqui, que não existem: o comando de conferência
#  devolveria erro justo na hora de conferir.)
# ---------------------------------------------------------------------

# 09/09/2026 — confirmado pelo bloco "COPIE DAQUI" do atualizar.sh, colado
# pelo dono: commit f86cd3a, serviço active, /health 200, migrações 001 a 046
# aplicadas. Estava parado em 037 desde 27/08 — nove migrações subiram de uma
# vez (038 a 046), junto com seis lotes de código de servidor.

# 10/09/2026 — 046 → 048.
#
# O dono colou o bloco COPIE DAQUI do atualizar.sh:
#
#     commit no servidor : 1286831
#     serviço            : active
#     /health local      : 200
#     migrações aplicadas: (...) 047_frete_tabela_e_km.sql,048_setor_qualidade.sql
#
# Sobe por causa DESSA prova, não por dedução. Conferido também por
# conteúdo: o commit 1286831 contém numerosDaFila (a correção do arrasto),
# a rota /api/frete/tabela, soAcompanha (o setor Qualidade) e os campos de
# KM na carga — e NÃO contém KM_FALTANDO, que é a trava que ele mandou
# tirar.
#
# O portão estava imprimindo 047 e 048 como pendentes depois de elas já
# terem rodado. Aviso falso gasta a credibilidade do aviso verdadeiro, que
# é a única coisa que este arquivo existe para proteger.
